name: Build & Release Android App

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version_number:
        description: 'Enter the release version (e.g., v1.0.0)'
        required: true
        type: string
      generate_release_notes:
        description: 'Generate human-readable, user-friendly release notes automatically?'
        type: boolean
        required: false
        default: false

jobs:
  release:
    name: Build & Release Android App
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: false

      - name: Grant Execute Permissions
        run: chmod +x ./gradlew

      - name: Determine Version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            RAW_VER="${{ github.event.inputs.version_number }}"
            echo "GENERATE_NOTES=${{ github.event.inputs.generate_release_notes }}" >> $GITHUB_ENV
          else
            RAW_VER="${{ github.ref_name }}"
            echo "GENERATE_NOTES=true" >> $GITHUB_ENV
          fi
          CLEAN_VER=$(echo "$RAW_VER" | sed 's/^v//')
          VAL_CODE=$((100 + ${{ github.run_number }}))
          echo "VERSION=$RAW_VER" >> $GITHUB_ENV
          echo "CLEAN_VERSION=$CLEAN_VER" >> $GITHUB_ENV
          echo "VERSION_CODE=$VAL_CODE" >> $GITHUB_ENV

      - name: Decode Keystore (Secure CI/CD)
        env:
          SIGNING_KEY_BASE64: ${{ secrets.SIGNING_KEY }}
        run: |
          if [ -n "$SIGNING_KEY_BASE64" ]; then
            echo "$SIGNING_KEY_BASE64" | base64 -di > app/signing-key.jks
          fi

      - name: Build Split Release APKs
        env:
          ANDROID_SIGNING_KEY_FILE: "signing-key.jks"
          ANDROID_SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
          ANDROID_SIGNING_KEY_ALIAS: ${{ secrets.SIGNING_KEY_ALIAS }}
          ANDROID_SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
          APP_VERSION_NAME: ${{ env.CLEAN_VERSION }}
          APP_VERSION_CODE: ${{ env.VERSION_CODE }}
        run: |
          ./gradlew assembleRelease \
            --build-cache \
            --no-daemon \
            --parallel \
            -Dorg.gradle.jvmargs="-Xmx4g -XX:+UseParallelGC" \
            -Dkotlin.compiler.execution.strategy="in-process" \
            -Pandroid.enablePngCrunching=false

      - name: Rename & Package Artifacts
        run: |
          mv app/build/outputs/apk/release/app-arm64-v8a-release*.apk ./locked-secure-${{ env.VERSION }}-arm64-v8a.apk
          mv app/build/outputs/apk/release/app-armeabi-v7a-release*.apk ./locked-secure-${{ env.VERSION }}-armeabi-v7a.apk
          mv app/build/outputs/apk/release/app-x86-release*.apk ./locked-secure-${{ env.VERSION }}-x86.apk
          mv app/build/outputs/apk/release/app-x86_64-release*.apk ./locked-secure-${{ env.VERSION }}-x86_64.apk

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          name: Release ${{ env.VERSION }}
          draft: false
          prerelease: false
          generate_release_notes: ${{ env.GENERATE_NOTES }}
          files: |
            locked-secure-${{ env.VERSION }}-arm64-v8a.apk
            locked-secure-${{ env.VERSION }}-armeabi-v7a.apk
            locked-secure-${{ env.VERSION }}-x86.apk
            locked-secure-${{ env.VERSION }}-x86_64.apk
